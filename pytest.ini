[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests without Neo4j dependency
    integration: Integration tests requiring Neo4j
    smoke: End-to-end smoke tests
    slow: Slow tests (full import, massive data)
